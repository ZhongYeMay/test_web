(()=>{const control=document.getElementById('scrollProgress');const value=document.getElementById('scrollProgressValue');if(!control||!value)return;let timer=null;const update=()=>{const root=document.documentElement;const max=Math.max(0,root.scrollHeight-window.innerHeight);const percent=max===0?100:Math.min(100,Math.max(0,Math.round((window.scrollY/max)*100)));control.style.setProperty('--page-progress',`${percent}%`);value.textContent=`${percent}%`;control.setAttribute('aria-label',`页面已滚动 ${percent}%，返回顶部`)};const schedule=()=>{if(timer!==null)return;timer=window.setTimeout(()=>{timer=null;update()},50)};control.addEventListener('click',()=>{const reduce=window.matchMedia('(prefers-reduced-motion: reduce)').matches;window.scrollTo({top:0,behavior:reduce?'auto':'smooth'})});window.addEventListener('scroll',schedule,{passive:true});window.addEventListener('resize',schedule,{passive:true});update()})();