(()=>{const trigger=document.getElementById('shortcutTrigger');const dialog=document.getElementById('shortcutDialog');const close=document.getElementById('shortcutClose');if(!trigger||!dialog)return;const open=()=>{if(typeof dialog.showModal==='function'){dialog.showModal();close?.focus()}else{dialog.setAttribute('open','')}};const closeDialog=()=>{if(typeof dialog.close==='function')dialog.close();else dialog.removeAttribute('open')};trigger.addEventListener('click',open);close?.addEventListener('click',closeDialog);dialog.addEventListener('click',event=>{if(event.target===dialog)closeDialog()});document.addEventListener('keydown',event=>{const target=event.target;const typing=target instanceof HTMLInputElement||target instanceof HTMLTextAreaElement||target instanceof HTMLSelectElement||target?.isContentEditable;if(typing||!event.altKey||!event.shiftKey||event.ctrlKey||event.metaKey)return;const key=event.key.toLowerCase();if(!['r','m','h','t'].includes(key))return;event.preventDefault();if(dialog.open)closeDialog();if(key==='r'){document.getElementById('runTest')?.click();document.getElementById('diagnostics')?.scrollIntoView({behavior:matchMedia('(prefers-reduced-motion: reduce)').matches?'auto':'smooth',block:'start'})}if(key==='m')document.getElementById('visitorMap')?.scrollIntoView({behavior:matchMedia('(prefers-reduced-motion: reduce)').matches?'auto':'smooth',block:'start'});if(key==='h')location.href='updates.html';if(key==='t')scrollTo({top:0,behavior:matchMedia('(prefers-reduced-motion: reduce)').matches?'auto':'smooth'})})})();